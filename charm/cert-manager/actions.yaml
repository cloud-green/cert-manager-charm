ca-cert:
  description: Get CA root certificate
  params: {}
  required: []
  additionalProperties: false
client:
  description: Get client certificate and key
  params:
    common-name:
      type: string
      description: common_name specified for client in config
  required: [common-name]
  additionalProperties: false
server:
  description: Get server certificate and key
  params:
    common-name:
      type: string
      description: common_name specified for server in config
  required: [common-name]
  additionalProperties: false
